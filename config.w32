// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("binpack", "for binpack support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("binpack", "enable binpack support", "no");

// TODO: for win32
if (PHP_BINPACK != "no") {
	EXTENSION("binpack", "php_binpack.c");
}

